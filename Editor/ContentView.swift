import SwiftUI

struct ContentView: View {

    var body: some View {
        VStack {
            Text("🐶 Dog Engine")
                .font(.largeTitle)

            Text("2D & 3D Game Engine")
                .foregroundColor(.gray)
        }
        .frame(minWidth: 900, minHeight: 600)
    }
}
